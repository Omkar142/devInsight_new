{
  "name": "devinsight-clone-server",
  "version": "1.0.0",
  "description": "Backend for DevInsight clone",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest",
    "clean": "rimraf node_modules package-lock.json",
    "setup": "npm install",
    "install:all": "npm run clean && npm run setup"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.0",
    "helmet": "^7.0.0",
    "mongoose": "^7.6.3",
    "node-cron": "^3.0.2",
    "swagger-ui-express": "^5.0.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  }
}